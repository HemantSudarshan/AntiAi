# TruthTracker API Configuration

# Environment
ENVIRONMENT=development  # development | production

# API Security
API_KEY=dev-key-change-in-production  # Change this in production!

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000  # Add production domains

# Server Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=true  # Set to false in production

# Model Configuration
MODEL_PATH=/path/to/models  # Optional: Path to pre-trained model weights
